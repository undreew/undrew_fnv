const PRODUCTS = {
	INDEX: '/products',
	SEARCH: '/products/search',
	CATEGORY: {
		INDEX: '/products/:category',
		VIEW: '/products/:category/:id',
	},
};

export const ROUTES = {
	INDEX: '/',
	PRODUCTS,
};
